# Installa il progetto stesso (cartella corrente) e, con esso, tutte le dipendenze
# dichiarate in pyproject.toml: rocketpy, numpy, scipy, matplotlib, pandas, tqdm.
# È il file che legge anche Streamlit Community Cloud al deploy.
# In locale, per lo sviluppo, conviene invece: pip install -e ".[app,dev]"
.
streamlit>=1.40
